{
  "name": "vitepress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dirScript": "node docs/scripts/index.js",
    "dev": "npm run dirScript && vitepress dev docs --open",
    "build": "npm run dirScript && vitepress build docs",
    "serve": "vitepress serve docs",
    "deploy": "bash deploy.sh",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "fs-extra": "^10.0.1",
    "vitepress": "^0.22.3"
  }
}
